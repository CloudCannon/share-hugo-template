# Metadata about this component, to be used in the CMS
spec:
  structures:
    - content_blocks
    - simple_blocks
  label: Headline
  description:
  icon:
  tags: []

# Defines the structure of this component, as well as the default values
blueprint:
  content:
    text: Headline
  style:
    heading_level: h1
    text_color: Primary

# Overrides any fields in the blueprint when viewing this component in the component browser
preview:
  content:
    text: BookShop Starter Library ipsum dolor sit amet scelerisque nisl.

# Any extra CloudCannon inputs configuration to apply to the blueprint
_inputs:
  heading_level:
    comment: The heading level, used for SEO. There should only be one h1 per page.
    type: select
    options:
      values:
        - h1
        - h2
        - h3
        - h4
  text_color:
    type: select
    options:
      values:
        - Primary
        - Secondary
